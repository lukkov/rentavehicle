{% extends 'admin/base_admin.html.twig' %}

{% block title %} Features {% endblock %}

{% block body %}
    {% include 'admin/_common/_filtering_form_view.html.twig' with {'filteringForm': featuresFilterForm, 'createUrl' : path('admin_features_create')} %}

    <div class="card">
        <div class="card-header card-header-primary">
            <h4 class="card-title pull-left">Features</h4>
            <h5 class="cart-title pull-right">Total count: {{ pager.nbResults }}</h5>
        </div>
        <div class="card-body">
            <div class="table-responsive">
                <table class="table">
                    <thead class=" text-primary">
                    <th>
                        ID
                    </th>
                    <th>
                        Name
                    </th>
                    <th>
                        Created at
                    </th>
                    <th>
                        Updated at
                    </th>
                    </thead>
                    <tbody>
                    {% for feature in features %}
                        <tr>
                            <td>
                                {{ feature.id }}
                            </td>
                            <td>
                                {{ feature.name }}
                            </td>
                            <td>
                                {{ feature.createdAt.format('d.m.Y. H:i:s') }}
                            </td>
                            <td>
                                {{ feature.updatedAt.format('d.m.Y. H:i:s')}}
                            </td>
                            <td>
                                <a href="{{ path('admin_features_update', {'id': feature.id}) }}"><i class="material-icons">edit</i></a>
                            </td>
                            <td>
                                <a href="#" data-href="{{ path('admin_features_delete', {'id': feature.id}) }}" data-toggle="modal" data-target="#confirm-delete"><i class="material-icons">close</i></a>
                            </td>
                        </tr>
                    {% endfor %}
                    </tbody>
                </table>
                {% include 'admin/_common/_pager.html.twig' %}
            </div>
        </div>
    </div>
{% endblock %}